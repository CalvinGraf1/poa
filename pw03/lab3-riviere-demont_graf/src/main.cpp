#include "people/noDrivers/childs/Son.hpp"
#include "people/noDrivers/childs/Daughter.hpp"
#include "people/noDrivers/Thief.hpp"
#include "Controller.hpp"

int main() {
	/*
    auto father = std::make_unique<Parent>("pere");
    auto mother = std::make_unique<Parent>("mere");
    auto paul = std::make_unique<Son>("paul", *mother,
												  *father);
    auto pierre = std::make_unique<Son>("pierre", *mother,
													 *father);
    auto julie = std::make_unique<Daughter>("julie",
														  *mother, *father);
    auto jeanne = std::make_unique<Daughter>("jeanne",
															*mother, *father);
    auto policeman = std::make_unique<Policeman>("policier");
    auto thief = std::make_unique<Thief>("voleur",
													  *policeman);

	 Controller people = {father.get(), mother.get(), paul.get(), pierre.get(),
								 julie.get(), jeanne.get(), policeman.get(), thief.get()};
	 people.run();*/
	 return 0;
}